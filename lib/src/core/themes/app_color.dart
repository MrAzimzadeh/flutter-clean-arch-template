import 'package:flutter/material.dart';

class AppColor {
  AppColor._();

  static const primary = Color(0xffF7F9FB);
  static const white = Color(0xFFFFFFFF);
  static const navy = Color(0xFF0C134F);
  static const blue = Color(0xFF1D267D);
  static const purple = Color(0xFF5C469C);
  static const lightPurple = Color(0xFFD4ADFC);
  static const lightGrey = Color(0xffF0EFEF);
  static const grey = Color.fromRGBO(180, 180, 180, 1);

  // Splash Screen Bg Color
  static const brightBlue = Color.fromRGBO(52, 103, 228, 1);
  static const blackTxt = Color.fromRGBO(28, 28, 28, 1);
  static const blackTxtSubtitle = Color.fromRGBO(28, 28, 28, 0.4);
  // Input Hint Text Color
  static const inputHintTextGrey = Color.fromRGBO(28, 28, 28, 0.2);
  static const inputBorder = Color.fromRGBO(28, 28, 28, 0.1);
  // Button Color
  static const buttonColor = Color.fromRGBO(0, 112, 225, 1);
  // Text Button Color
  static const textButtonColor = Color.fromRGBO(149, 164, 252, 1);

  // Side Menu Colors
  static const sideMenuTxtColor = Color.fromRGBO(52, 59, 79, 1);
  static const sideMenuHoverColor = Color.fromRGBO(229, 240, 254, 1);

  // App Bar Colors
  static const appBarTxt = Color.fromRGBO(52, 64, 84, 1);
  static const appBarSubTxt = Color.fromRGBO(159, 159, 159, 1);

  // Home Page Colors
  static const header = Color.fromRGBO(102, 112, 133, 1);
  static const containerBorder = Color.fromRGBO(234, 236, 240, 1);
  static const textFieldBorder = Color.fromRGBO(208, 213, 221, 1);
  static const shadowColor = Color.fromRGBO(0, 0, 0, 0.25);
  static const blueText = Color.fromRGBO(62, 98, 225, 1);
  static const lightGreen = Color.fromRGBO(5, 193, 104, 0.2);
  static const lightRed = Color.fromRGBO(255, 204, 206, 1);
  static const greenTxt = Color.fromRGBO(20, 202, 116, 1);
  static const redTxt = Color.fromRGBO(255, 31, 37, 1);
}